extends layout

block content
    h1= `${specifiedUser.username}'s Collections`
        ul
            each collection in collections
                h2= `${collection.name}`
                    li= collection
