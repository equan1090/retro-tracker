extends layout

block content
    h1= `${specifiedUser.username}'s Collections`
    ul
        each collection in collections
            h4: a(href=`/collections/${collection.id}`)= `${collection.name}`
